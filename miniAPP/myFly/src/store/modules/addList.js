import {addView} from "../../service/addList"
//原来数据
const state = {
    
};

//派生数据
const getters = {

};

//异步改变
const actions = {
    formSubmit({commit},e,payload){
        console.log(e)
    }
};

//同步改变
const mutations = {
   
};

export default {
  namespaced: true,
  state,
  getters,
  actions,
  mutations
};
